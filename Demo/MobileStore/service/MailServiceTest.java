package com.fa.service;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import com.fa.repository.RoleRepository;
import com.fa.repository.RoleUserRepository;
import com.fa.repository.UserRegisterRepository;
import com.fa.repository.UserRepository;

public class MailServiceTest {

  private MailServiceImpl mailService;


  /*
   * @Test public void confirmEmailExpire() {
   * 
   * mailService = new MailServiceImpl(); int result =
   * mailService.confirmEmail("11", "dat123@yopmail.com"); assertEquals(0,
   * result); }
   */
  /*
   * @Test public void confirmEmailException() { mailService = new
   * MailServiceImpl(); int result = mailService.confirmEmail("0",
   * "dat123@yopmail.com"); assertEquals(3, result); }
   */

}
